import { cpus } from 'os';
import { exec, fork, spawn } from 'child_process';

describe('CLI test suite', () => {
  it('Invokes the start command', () => {
    // exec('npm run cli -- start', () => {

    // });
  });
});
